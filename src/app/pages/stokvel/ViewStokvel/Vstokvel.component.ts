import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-Vstokvel', 
  templateUrl: './Vstokvel.html', 
  styleUrls: ['./Vstokvel.css'], 
  standalone: true,
  imports: [RouterLink]
})

export class VStokvelComponent {
}